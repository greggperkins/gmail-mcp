{
  "name": "@beam/gmail-mcp",
  "version": "0.1.0",
  "description": "The most complete Gmail MCP server. 42 tools across messages, threads, drafts, labels, attachments, filters, and settings.",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "gmail-mcp": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "mcp",
    "gmail",
    "email",
    "model-context-protocol",
    "ai",
    "claude",
    "typescript"
  ],
  "author": "BEAM (https://usebeam.sh)",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "google-auth-library": "^9.0.0",
    "googleapis": "^140.0.0",
    "html-to-text": "^9.0.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/html-to-text": "^9.0.0",
    "@types/node": "^20.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
